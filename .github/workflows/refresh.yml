# The "Updated" date on a card comes from the newest commit touching that prototype,
# read at build time. A prototype lives in another repo, so nothing here notices when
# it changes. This run rebuilds the gallery on a schedule and commits only when the
# result actually differs.
name: Refresh the gallery

on:
  schedule:
    - cron: "17 */3 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Rebuild index.html
        env:
          # build-gallery.py reads the source repos through the gh CLI. The run's
          # own token only sees public repos, so a private prototype repo needs
          # GALLERY_READ_TOKEN: a token with read access to effectory-ux. Without
          # it the build fails loudly rather than publishing a smaller gallery.
          GH_TOKEN: ${{ secrets.GALLERY_READ_TOKEN || github.token }}
        run: python3 build-gallery.py

      - name: Commit when something changed
        run: |
          if git diff --quiet -- index.html; then
            echo "No change."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Refresh the updated dates" -- index.html
          git push
